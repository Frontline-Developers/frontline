# Frontline — Project Context

> Deep reference for AI agents. Read `CLAUDE.md` first for architecture rules and conventions.
> This file covers the *what* and *why* — feature intent, team assignments, schemas, and the full task breakdown.

---

## 1. Project Overview

**Frontline** is a privacy-first, location-based citizen reporting and news platform. Users can:
- View a **Map Feed** of local events as interactive flutter_map markers with clustering
- Read a **Live Feed** combining citizen reports and GDELT wire news in a chronological split-timeline
- Submit **Anonymous Reports** via a multi-step form with camera/gallery media uploads
- Track their own submissions in a **My Reports** dashboard (local, sorted)
- **Confirm or Dispute** reports via a consensus/threshold voting system
- Leave **Comments** on reports (nested discussion threads)
- **Withdraw** their own reports at any time

**Core privacy guarantees (non-negotiable):**
- Coordinates are fuzzed ±3km server-side before any Firestore write
- No IP addresses logged anywhere — App Check enforces this
- Authentication is anonymous-only (Firebase Anonymous Auth)
- EXIF metadata stripped from all uploaded media via Cloud Functions + Sharp
- No PII stored: no names, emails, phones, or device identifiers

**Firebase project:** `frontline-549fb`
**Firestore region:** Singapore (`asia-southeast1`)
**Cloud Functions region:** Singapore (`asia-southeast1`)

---

## 2. Team & Responsibilities

| Member | Primary Role |
|---|---|
| **Oakar** | Lead — system architecture, backend infra, GeoFire, Firebase setup, DevOps |
| **Nicha** | Backend/Flutter — Firestore schemas, Anonymous Auth, fuzzing, Cloud Functions |
| **Natthanicha** | UI/UX design + Flutter — Figma mockups, Reporting Form, Event Detail view |
| **Kalyatorn** | UI/UX design + Flutter — design system, Map UI, Timeline, My Reports |
| **May** | Flutter/Mapbox + Backend — ThemeData, Mapbox SDK, GDELT, Cloud Functions |

---

## 3. Firestore Schema

### `reports/{reportId}`
```
userId:               string      ← anonymous Firebase UID (never linked to real identity)
location:             GeoPoint    ← fuzzed ±3km via fuzzReportLocation CF (NEVER raw coords)
geohash:              string      ← generated by geoflutterfire_plus for radius queries
category:             string      ← 'combat' | 'aid' | 'alert' | 'displaced' | 'infra' | 'other'
description:          string
mediaUrls:            string[]    ← Firebase Storage paths (EXIF stripped before upload)
status:               string      ← 'pending' | 'confirmed' | 'disputed' | 'withdrawn'
confirmCount:         number      ← human confirm votes; incremented by confirmReport CF
disputeCount:         number      ← human dispute votes; incremented by disputeReport CF
systemConfirms:       number      ← virtual confirm votes from evaluateReportTrust heuristics
systemDisputes:       number      ← virtual dispute votes from evaluateReportTrust heuristics
totalEffectiveVolume: number      ← C_eff + D_eff; written by confirmReport/disputeReport CF
confidenceRatio:      number      ← C_eff / V; written by confirmReport/disputeReport CF
isDisputed:           boolean     ← set by spatial-temporal conflict CF (4.12)
exifStripped:         boolean     ← true after client-side strip (CF re-verifies per task 5.1)
createdAt:            Timestamp
```

### `reports/{reportId}/interactions/{userId}`
```
type:         string      ← 'confirm' | 'dispute'
token:        string      ← unique vote token (prevents duplicate voting — task 5.8)
createdAt:    Timestamp
```

### `reports/{reportId}/comments/{commentId}`
```
type:         string      ← 'confirm' | 'dispute' | 'context'
text:         string      ← comment body (≤ 500 chars); field is 'text', NOT 'body'
authorToken:  string      ← short anonymous token, NOT a Firebase UID (privacy by design)
upvotes:      number
createdAt:    Timestamp
```

### `wire_news/{articleId}`
```
title:        string
body:         string?
url:          string
source:       'wire'
publishedAt:  Timestamp
geohash5:     string?   ← required by wire corroboration heuristic; populated when fetchGdeltNews is implemented
```
← written only by `fetchGdeltNews` CF; client read-only

---

## 4. Cloud Functions

| Function | Trigger | Task | Owner | Status |
|---|---|---|---|---|
| `fuzzReportLocation` | `onCall` (authenticated) | 4.7 | Nicha | Implemented |
| `fetchGdeltNews` | `onSchedule` every 30 min | 4.9, 4.10 | May, Oakar | Stub |
| `stripExifMetadata` | `onObjectFinalized` (Storage) | 5.1 | Oakar | Pending |
| `evaluateReportTrust` | `onDocumentCreated` (reports) | 4.11, 4.12 | Nicha | Implemented |
| `confirmReport` | `onCall` (authenticated) | 4.11, 5.6 | Nicha | Implemented |
| `disputeReport` | `onCall` (authenticated) | 4.11, 5.6 | Nicha | Implemented |
| `checkSpatialConflict` | `onDocumentCreated` (reports) | 4.12 | May | Superseded by evaluateReportTrust heuristics |
| `withdrawReport` | `onCall` (authenticated) | 5.9 | Nicha | Pending |

All functions deploy to `asia-southeast1` (Singapore).

---

## 5. Key Feature Notes

### Map Feed (features/map)
- flutter_map + OpenStreetMap tiles — custom marker layers per category, optional clustering via `flutter_map_marker_cluster`. Picker (reporting flow) switched from Mapbox Flutter SDK after v2.24.3 was found to have no Flutter-web support (rendered blank canvas under DDC).
- GeoFire radius query (`geoflutterfire_plus`) on `reports` collection using bounding-box + geohash
- Filters by category and time range
- Tapping a cluster expands; tapping a marker opens Event Detail
- Tasks: 3.5, 3.6, 5.5

### Live Feed (features/feed)
- Split side-by-side chronological timeline: left column = citizen reports, right = wire news
- Pull-to-refresh, scrollable `ListView.builder`
- Citizen items from `reports/` (Firestore real-time listener), wire items from `wire_news/`
- Visually distinct card components per source type
- Tasks: 3.7, 3.8, 3.9, 5.4

### Anonymous Reporting (features/reporting)
- Multi-step form: location → category → description → media upload → review → submit
- Camera/gallery via `image_picker` with permission handling
- Before submit: call `fuzzReportLocation` CF → receive fuzzed coords → write to Firestore
- Media upload to Storage (`reports/{userId}/{reportId}/{file}`) → `stripExifMetadata` CF fires automatically
- Input validation at each step, guard against double-submit
- Tasks: 3.10, 3.11, 4.7, 5.1, 5.2, 5.3

### Event Detail (features/reporting — detail screen)
- Full-screen view of a single report
- Confirm / Dispute buttons → call respective Cloud Functions
- Nested comment thread (Firestore real-time listener on `comments/`)
- Token-based voting to prevent duplicate confirms/disputes (task 5.8)
- "Disputed" banner if `isDisputed == true` (set by spatial-temporal conflict CF)
- Tasks: 3.12, 5.6, 5.7

### My Reports (features/my_reports)
- Queries `reports/` where `userId == currentUser.uid`
- Local sort by date / status
- "Withdraw" button → calls `withdrawReport` CF → deletes report + media
- Drift local cache for offline access (task 5.10) + retry queue (task 5.11)
- Tasks: 3.13, 5.9, 5.10, 5.11

### Consensus & Disputed Flags
- `confirmReport` / `disputeReport` CFs increment counts atomically
- Threshold logic in CF determines when `status` flips to 'confirmed' or 'disputed'
- `checkSpatialConflict` CF fires on new report creation — checks for spatially/temporally nearby conflicting reports and sets `isDisputed`
- Tasks: 4.11, 4.12

---

## 6. Full Work Breakdown Structure

### Phase 1 — Project Planning & System Architecture
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 1.1 | Define project scope, core features, privacy reqs | Oakar | 0.4 |
| 1.2 | Draft Firestore NoSQL Entity-Relationship Diagram | Oakar | 0.5 |
| 1.3 | Design client-server data flow & architecture diagrams | Oakar | 0.6 |
| 1.4 | Initialize tracking boards (Jira/Trello) & GANTT chart | Nicha | 0.3 |
| 1.5 | Establish version control guidelines & init Git repo | Nicha | 0.3 |

### Phase 2 — UI/UX Design & Prototyping (Figma)
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 2.1 | Define global design system (colors, typography, mode tokens) | Natthanicha | 0.6 |
| 2.2 | Design core reusable UI components (nav, buttons, inputs) | Kalyatorn | 0.8 |
| 2.3 | Design custom SVG map marker assets & cluster indicators | Natthanicha | 0.5 |
| 2.4 | Create low-fidelity wireframes — Map, Feed, Form layouts | Kalyatorn | 0.8 |
| 2.5 | Develop high-fidelity mockups — Map View & UI overlays | Natthanicha | 1.0 |
| 2.6 | Develop high-fidelity mockups — Citizen & Wire News cards | Kalyatorn | 0.6 |
| 2.7 | Develop high-fidelity mockups — Anonymous Reporting Form | Natthanicha | 0.8 |
| 2.8 | Assemble interactive Figma prototype connecting all screens | Kalyatorn | 0.6 |

### Phase 3 — Frontend Development (Flutter)
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 3.1 | Initialize Flutter project, remove boilerplate, configure deps | Nicha | 0.4 |
| 3.2 | Set up app routing architecture (GoRouter) & define core routes | Oakar | 0.5 |
| 3.3 | Initialize global state management (Riverpod) & define models | Oakar | 0.8 |
| 3.4 | Translate Figma design tokens into global Flutter ThemeData | May | 0.5 |
| 3.5 | Install and configure Mapbox Flutter SDK with platform API keys | May | 0.5 |
| 3.6 | Implement Mapbox marker rendering, dynamic clustering, filters | May | 1.3 |
| 3.7 | Build scrollable Live Feed list view with pull-to-refresh | Nicha | 1.0 |
| 3.8 | Construct visually distinct UI components for reports & news | Kalyatorn | 0.8 |
| 3.9 | Build split Side-by-Side chronological timeline view | Kalyatorn | 1.3 |
| 3.10 | Build multi-step Reporting Form wrapper with input validation | Natthanicha | 1.0 |
| 3.11 | Implement device camera/gallery permissions & preview UI | Kalyatorn | 0.6 |
| 3.12 | Build full-screen Event Detail view with nested discussion threads | Natthanicha | 1.0 |
| 3.13 | Create localized My Reports dashboard sorting submissions | Kalyatorn | 0.8 |

### Phase 4 — Backend Development & Core Logic (Firebase)
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 4.1 | Initialize Firebase project & configure Cloud Functions env | Oakar | 0.4 |
| 4.2 | Set up Firestore Security Rules & establish GeoFire collections | Oakar | 0.6 |
| 4.3 | Implement core Firestore schemas (Event, Report, Interactions) | Nicha | 0.8 |
| 4.4 | Configure GeoFire indexing for optimized coordinate queries | Oakar | 0.9 |
| 4.5 | Set up Firebase Cloud Storage for sanitized media uploads | Oakar | 0.6 |
| 4.6 | Implement Firebase Anonymous Auth & flutter_secure_storage | Nicha | 0.8 |
| 4.7 | Write location fuzzing algorithm (+/- 3km radius randomization) | Nicha | 0.9 |
| 4.8 | Configure Firebase App Check & Security Rules for No IP Logging | Nicha | 0.5 |
| 4.9 | Integrate GDELT API & write Cloud Functions parsing utilities | May | 1.2 |
| 4.10 | Set up Cloud Scheduler to automatically fetch wire news | Oakar | 0.6 |
| 4.11 | Write Cloud Functions consensus logic for verification thresholds | Nicha | 1.3 |
| 4.12 | Implement spatial-temporal conflict logic for "Disputed" flags | May | 1.1 |

### Phase 5 — API Integration & Application Logic
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 5.1 | Implement EXIF stripping using Cloud Functions & Sharp | Oakar | 0.6 |
| 5.2 | Connect Flutter form to Firestore & Cloud Storage for media | Nicha | 0.9 |
| 5.3 | Serialize form payload data (descriptions, categories, locations) | Nicha | 0.4 |
| 5.4 | Connect Flutter Live Feed using Firestore Real-time Listeners | Nicha | 0.8 |
| 5.5 | Extract map bounding-box coordinates & query events via GeoFire | Nicha | 0.9 |
| 5.6 | Connect Confirm/Dispute buttons to Cloud Functions validation | Nicha | 0.8 |
| 5.7 | Connect Flutter comment input field to Firestore Real-time DB | Nicha | 0.5 |
| 5.8 | Implement validation logic to prevent duplicate token-based voting | Oakar | 0.6 |
| 5.9 | Connect "Withdraw Report" button to Firestore Delete & cleanup | Nicha | 0.5 |
| 5.10 | Configure local caching using Drift & Firestore Offline Cache | Oakar | 0.9 |
| 5.11 | Implement offline action queue to save/retry failed submissions | Oakar | 1.1 |

### Phase 6 — Quality Assurance, Privacy Audits & Testing
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 6.1 | Intercept client payloads to verify EXIF stripping in Functions | Oakar | 0.5 |
| 6.2 | Audit Firestore logs & DB records to guarantee zero IP retention | Oakar | 0.5 |
| 6.3 | Run geographic distribution tests on location fuzzing output | Nicha | 0.6 |
| 6.4 | Write Cloud Functions unit tests (consensus & fuzzing logic) | Oakar | 1.3 |
| 6.5 | Profile Flutter memory usage & frame-rates under extreme load | Oakar | 0.6 |
| 6.6 | Perform cross-platform UI rendering checks on physical iOS devices | Kalyatorn | 0.5 |
| 6.7 | Perform cross-platform UI checks on physical Android devices | Natthanicha | 0.5 |

### Phase 7 — Deployment, DevOps & Project Handover
| ID | Task | Owner | Est. Days |
|---|---|---|---|
| 7.1 | Configure GitHub Actions pipeline for automated testing & builds | Oakar | 0.9 |
| 7.2 | Provision production Firestore & configure Firebase Hosting envs | Oakar | 0.6 |
| 7.3 | Finalize inline codebase commenting, README & API documentation | May | 0.8 |
| 7.4 | Draft, format and compile final project report & presentation | Natthanicha, Kalyatorn | 1.3 |

**Total estimated effort: ~539 hours across 5 developers**

---

## 7. Timeline

| Phase | Dates | Status |
|---|---|---|
| Phase 1 — Architecture | May 22–23 | Complete |
| Phase 2 — UI/UX Design | May 22–28 | Complete |
| Phase 3 — Flutter Frontend | Late May → Jun 2 | In Progress |
| Phase 4 — Firebase Backend | May 24 → Jun 1 (concurrent) | In Progress |
| Phase 5 — Integration | Jun 1–5 | In Progress |
| Phase 6 — QA & Privacy | May 24 → Jun 4 (rolling) | In Progress |
| Phase 7 — Deploy & Handover | Jun 3–5 | Upcoming |

**Target launch: Early June 2026**

### Implemented as of Jun 4 2026
- Compare page (`/compare`) — full Clean Architecture feature: real-time `watchClusters()` stream groups all citizen + wire items by category+date; timeline UI with SUPPORTS/CONTRADICTS/UNVERIFIED evidence badges; anchor mode (launched from Feed "Compare" button) filters to matching-category clusters and shows FeaturedItemCard
- `FetchRelatedWireNewsUseCase` — three-tier fallback (location match → category match → recency) for future single-report wire-news lookup; domain layer only, wired into `CompareRepository`
- `EventDetailScreen` (`/report/:id`) — full-screen detail view for both citizen reports and wire news; citizen branch shows hero image, category+status badges, community verification panel (confirm/dispute meter + vote buttons), compare CTA, and discussion preview; wire branch shows article summary, source name, "Read full article" button (opens URL in browser), and compare CTA; Feed cards navigate to this screen via `context.push('/report/:id', extra: item)`
- Test suite: 176 tests across 22 files covering auth, feed, map, my_reports, comments, compare (4 files), reporting + EventDetailScreen (21 tests)

---

## 8. Additional Technical Decisions

### Offline Support (tasks 5.10, 5.11)
- `drift` package for local SQLite cache of user's own reports
- Offline action queue: if `submitReport` fails (no network), save to local queue and retry on reconnect
- Firestore SDK offline persistence handles read caching automatically

### EXIF Stripping (task 5.1)
- All media uploads go to Storage → `onObjectFinalized` CF fires → `sharp` library strips EXIF → overwrites file
- Flutter client must NOT display media until CF confirms strip is complete (check a `exifStripped: true` field on the report doc)

### Consensus Thresholds (tasks 4.11, 5.6) — Hybrid Consensus Model
- Human votes combined with automated System Trust Scores from `evaluateReportTrust` heuristics
- `C_eff = confirmCount + systemConfirms`, `D_eff = disputeCount + systemDisputes`, `V = C_eff + D_eff`
- V < 5 → `pending`; V ≥ 5 && C_eff/V ≥ 0.75 → `confirmed`; V ≥ 5 && D_eff/V ≥ 0.60 → `disputed`
- System scores: EXIF GPS match = +3 systemConfirms; wire news corroboration = +2 systemConfirms; spatial spike (flash mob) = +2 systemConfirms cluster-wide; impossible travel (>900 km/h) = +5 systemDisputes
- Token-based: each user gets one vote per report; token stored in `interactions/{userId}`; CF checks existence before incrementing

### "Disputed" Flag Logic (task 4.12)
- Spatial: report within X km of an existing conflicting report
- Temporal: within Y hours of the conflicting report
- If both conditions met, `isDisputed` = true; shown as warning banner in Event Detail

### App Check (task 4.8)
- Android: Play Integrity API
- Web: reCAPTCHA v3
- Enforced on all callable Cloud Functions
- Purpose: prevent unauthorized API calls and ensure no IP logging via unregistered clients

### Map Tiles
- flutter_map fetches OpenStreetMap tiles via `https://tile.openstreetmap.org/{z}/{x}/{y}.png` — no API key required.
- `userAgentPackageName: 'app.frontline.mobile'` is set on the `TileLayer` per OSM tile-usage policy.
- For higher-volume usage, consider switching to a Mapbox raster tile URL with a token at build time
- Retrieved in Dart via `const String.fromEnvironment('MAPBOX_ACCESS_TOKEN')`
- Never hardcoded in source files

---

## 9. Pending / TODO Items

- [x] Firestore schema — GeoFire geohash field added to `reports` (task 4.4)
- [x] Firestore indexes defined in `firestore.indexes.json` (reports: geohash+category+createdAt, reports: userId+createdAt, wire_news: geohash5+publishedAt)
- [x] `confirmReport` and `disputeReport` Cloud Functions implemented with Hybrid Consensus Model
- [x] `evaluateReportTrust` Cloud Function implemented (4 heuristics: EXIF, wire news, spatial spike, impossible travel)
- [ ] `fetchGdeltNews` must populate `geohash5` field on `wire_news` documents for wire corroboration heuristic to activate
- [ ] `gdelt.ts` `initializeApp` guard can be removed once it imports from `admin.ts` (safe — both guards use `!admin.apps.length`)
- [ ] Firestore rules `allow read` on `reports/` is currently owner-only — needs broadening for Event Detail (confirm/dispute) flow (task 5.6) — **EventDetailScreen UI is done; unblocked once rules are updated**
- [ ] `/report/:id` route relies on GoRouter `extra` to receive `NewsItem` — deep links and process restarts fall back to `_ReportDetailPlaceholder` until a Firestore-backed fetch is wired (task 5.7)
- [ ] Additional Cloud Functions to be added: `stripExifMetadata`, `withdrawReport`
- [ ] `drift` dependency to be added to `pubspec.yaml` when implementing task 5.10
- [ ] Firebase App Check to be initialized in `main.dart` (task 4.8)
- [ ] No map token needed for OpenStreetMap tiles (flutter_map); if switching to Mapbox raster tiles, token would go in `apps/mobile/.env`
- [ ] Figma design tokens to be implemented in `AppTheme` / `AppColors` (task 3.4)
- [ ] Map feature (`/`) is a stub — Mapbox/flutter_map integration still needed (tasks 3.5, 3.6, 5.5)
- [ ] My Reports withdraw button to be connected (task 5.9)
- [ ] Offline cache via Drift not yet implemented (tasks 5.10, 5.11)
- [ ] Compare page grouping uses category+date — can be upgraded to geo-spatial clustering once Map geo queries are live
